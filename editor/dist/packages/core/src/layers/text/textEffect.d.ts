import { CSSObject } from '@emotion/react';
import { EffectSettings } from '@lidojs/core';
export declare const getTextEffectStyle: (effect: string, settings: EffectSettings, textColor: string, fontSize: number) => CSSObject;
